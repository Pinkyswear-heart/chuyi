# torch / torchvision 由 AutoDL PyTorch 镜像预装（本项目需 torch>=2.0）
# 本地首次安装: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#               (纯 CPU: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu)
numpy>=1.24
tqdm>=4.60
matplotlib>=3.5
tensorboard>=2.15
